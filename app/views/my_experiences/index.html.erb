<h1>MyExperiences#index</h1>
<div>
  <%= render 'shared/search' %>
</div>
<br>
<div class="container">
  <div class="row">
    <%# i want the space that will be with 2/3 to cards and 1/3 to the map %>
    <div class="col-8"> <%# grid of cards %>
      <div class="container d-flex align-items-center justify-content-center">
        <% @my_experiences.each do |my_experience|%>
          <%= render 'shared/card_experience', experience: my_experience.experience %>
        <% end %>
      </div>
    </div>
  </div>
</div>
