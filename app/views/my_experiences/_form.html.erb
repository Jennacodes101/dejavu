<%= simple_form_for [@experience, @my_experience] do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.input :done %>
  <%= f.submit "Add to list", class: 'rounded-pill btn btn-dark shadow-sm' %>
<% end %>
